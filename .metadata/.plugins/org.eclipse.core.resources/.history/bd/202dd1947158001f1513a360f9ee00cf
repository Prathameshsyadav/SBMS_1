package in.ashokit.service;

import java.util.List;
import java.util.Optional;

import org.springframework.stereotype.Service;

import in.ashokit.entity.Student;
import in.ashokit.repo.StuRepo;

@Service
public class StuService {
	
	private StuRepo stuRepo;

	public StuService(StuRepo stuRepo) {
		this.stuRepo = stuRepo;
	}
	
	public void getAllStudents() {
		Iterable<Student> all = stuRepo.findAll();
		all.forEach(System.out::println);
	}
	
	public void getStudentById(Integer id) {
		Student student = stuRepo.findById(id).get();
		System.out.println(student);
		
	}
	
/*	public void getStudentByCity(String s) {
		List<Student> address = stuRepo.findByAddress(s);
		System.out.println(address);
	}
	
	public void getStudentsRollNo(Integer id) {
		List<Student> stu = stuRepo.findBySrollNoGreaterThan(id);
		stu.forEach(System.out::println);
	}
	
	public void getStu(String add, String name) {
		List<Student> list = stuRepo.findByAddressAndSname("MUM", "Ramesh");
		list.forEach(e -> System.out.println(e));
	}
	*/
	
	//public void updateStudent() {
		//stuRepo.updateStu(23, 1);
		
	//}
	
//	public void deleteStudent() {
//		stuRepo.deleteStu(1);
//	}
	
	public void saveStu() {
		stuRepo.saveStudent(9, "Akash", 99, "Ambernath");
	}

}
